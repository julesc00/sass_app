[project]
name = "sass-app"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "fastapi>=0.136.1",
    "pydantic>=2.13.3",
    "pytest-cov>=7.1.0",
    "python-decouple>=3.8",
    "sqlalchemy>=2.0.49",
    "uvicorn>=0.46.0",
]
